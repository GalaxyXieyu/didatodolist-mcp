# MCP 服务鉴权（客户端连接需要在请求头携带 x-api-key）
MCP_API_KEY=

# OAuth 配置（推荐使用一次性授权脚本写入 token）
# 以下两项来自滴答清单开放平台
DIDA_CLIENT_ID=
DIDA_CLIENT_SECRET=

# 回调地址（本地授权默认 38000 端口）
DIDA_REDIRECT_URI=http://localhost:38000/callback

# 以下两项由认证流程生成（不建议手工填写；oauth_authenticate.py 可写入 .env）
#DIDA_ACCESS_TOKEN=
#DIDA_REFRESH_TOKEN=

# 可选：自定义配置文件路径（若仍要配合 oauth_config.json 使用）
#OAUTH_CONFIG_PATH=oauth_config.json
